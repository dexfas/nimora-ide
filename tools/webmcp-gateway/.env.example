# Copy values into your shell/environment. Do not commit a real .env file.
SHUNCODE_MCP_URL=https://your-bridge.example/mcp/<route-token>
# Optional. If changed, package the same value into the Personal Edge Bridge.
SHUNCODE_PERSONAL_EDGE_TOKEN=shuncode-local-development
